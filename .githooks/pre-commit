#!/bin/bash
# Pre-commit hook: 빠른 스타일 검증 (spotless)
# 실패 시 커밋 차단

echo "🔍 Pre-commit: spotless 검사 실행 중..."

./gradlew spotlessCheck --daemon -q

if [ $? -ne 0 ]; then
    echo "❌ spotless 검증 실패. ./gradlew spotlessApply 실행 후 다시 커밋하세요."
    exit 1
fi

echo "✅ Pre-commit: spotless 통과"
